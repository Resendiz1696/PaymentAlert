<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.PaymentAlert" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.PaymentAlert" parent="Base.Theme.PaymentAlert" />

    <style name="Theme.PaymentAlert.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.PaymentAlert.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.PaymentAlert.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>